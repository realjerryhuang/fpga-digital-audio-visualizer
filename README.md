Digital Audio Visualizer

This passes analog microphone inputs through an ADC to receive digital time-domain signals with which an N-point FFT can be performed. The output frequency signals are visualized as a histogram on a VGA display.

To be run on Vivado with ADC IP.